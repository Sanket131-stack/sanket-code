import requests
from bs4 import BeautifulSoup
import pandas as pd
import sqlite3

# Data collection
def collect_from_api():
    url = "client api link"  # client  API
    response = requests.get(url)
    if response.status_code == 200:
        data = response.json()
        return data
    return []

